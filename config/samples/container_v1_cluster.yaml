apiVersion: container.cnrm.cloud.google.com/v1
kind: Cluster
metadata:
  labels:
    app.kubernetes.io/name: container-kcc
    app.kubernetes.io/managed-by: kustomize
  name: cluster-sample
spec:
  # TODO(user): Add fields here
